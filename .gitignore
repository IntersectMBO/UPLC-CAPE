.vscode/
.direnv/
node_modules/
dist-newstyle/
*.local.md
.hspec-failures
.envrc.local
build-and-deploy.sh
result
result-*
*.code-workspace
.direnv
.local
worktrees/

# Generated HTML report. CI (static.yml on main, pr-ci.yml on PRs) runs
# `cape submission report --all` and deploys to gh-pages — the tracked
# snapshot in main is redundant and only produces PR-diff noise.
report/
