Usage:
  cape test [options]

Description:
  Runs the complete UPLC CAPE test suite to verify CLI commands and scripts.
  This includes testing benchmark creation, submission management,
  verification (correctness + schemas), measurement, and reports.


Options:
  -h, --help    Show this help message

Examples:
  cape test           # Run full test suite
  cape test --help    # Show this help

The test suite runs in a sandboxed environment and does not modify your
actual project files. It tests:
- CAPE CLI functionality (benchmark, submission commands)
- Template-based file creation
- Verification and measurement workflows
- Directory structure validation
- UPLC submission validation against test suites

Note: Run from within the nix development shell.
